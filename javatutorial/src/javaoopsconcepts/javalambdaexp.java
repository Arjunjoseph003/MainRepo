package javaoopsconcepts;
//import java.util.ArrayList;
//import java.util.function.Consumer;
//interface Stringfunction {
	//String run (String Str);
//}

public class javalambdaexp {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*ArrayList <Integer> numbers = new ArrayList<Integer>();
		numbers.add(4);
		numbers.add(5);	
		numbers.add(6);
		numbers.add(7);
		numbers.add(8);
		numbers.forEach((n) -> {System.out.println(n);} );// direct values assignment through lambda expressions
		Consumer <Integer> method = (n) -> {System.out.println(n);};// values are assigned through variables with the help of consumers class from the java built in packages 
		numbers.forEach(method);
				Stringfunction exclaim = (s) -> s + "!";
				Stringfunction ask = (s)-> s + "?";
				printformatted("hello",exclaim);
				printformatted("hello", ask);
				
				
		}
			public static void printformatted(String Str, Stringfunction format) {
				String result = format.run(Str);
				System.out.println(result);*/
	}

}
